import React from 'react';
import { text, number } from '@storybook/addon-knobs';

import Toolbar from './toolbar';

export default { title: 'Toolbar' };

export const simple = () => <Toolbar></Toolbar>;

